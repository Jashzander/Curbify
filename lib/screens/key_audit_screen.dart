import 'package:flutter/material.dart';

class KeyAuditScreen extends StatelessWidget {
  const KeyAuditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Key Audit'),
      ),
      body: const Center(
        child: Text('Key Audit Screen'),
      ),
    );
  }
}
